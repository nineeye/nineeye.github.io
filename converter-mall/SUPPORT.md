# 지원 및 문제 해결

1. 화면이 갱신되지 않으면 서버가 바라보는 프로젝트 폴더가 최신본인지 확인합니다.
2. 브라우저에서 `Ctrl + Shift + R`로 강력 새로고침합니다.
3. PNG → JPG 화면의 `환경 점검`을 실행하고 진단 JSON을 저장합니다.
4. 문제 재현 시 브라우저 종류·버전, 파일 수, 파일 크기, 진단 JSON을 함께 기록합니다.

이미지 원본 자체는 외부 서버로 전송하지 않습니다.
